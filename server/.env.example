# URL of your Cloudflare Worker RSS proxy (optional, falls back to direct fetch)
RSS_PROXY_URL=https://your-worker.workers.dev

# Path to Firebase service account JSON (downloaded from Firebase Console)
# Place the file as server/service-account.json
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
